apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.instanceName }}-workspace
  namespace: {{ .Values.instanceName }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.storage.size }}
  storageClassName: {{ .Values.storage.storageClassName }}
