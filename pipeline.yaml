settings:
  verbose: true
  shell_init: "source ./.alias" 
steps:
  - name: "clean_workspace"
    mode: "foreground"
    script: |
      mkdir -p ./terraform/out
      rm -rf ./terraform/out/*
      echo "🧹 Workspace cleaned."
  - name: "infrastructure"
    mode: "foreground"
    script: |
      set -e
      cd terraform
      terraform init
      # Function to truly wipe Multipass ghosts
      wipe_multipass() 
      {
        echo "🧹 Nuking Multipass ghosts..."
        set -x
        multipass stop --all || true
        multipass delete --all || true
        multipass purge || true
        # Also remove local terraform state to force a fresh start
        rm -rf terraform.tfstate terraform.tfstate.backup .terraform.lock.hcl
        terraform init
        set +x
      }
      if [ "$FORCE_REBUILD" == "true" ]; then
        wipe_multipass
      fi
      echo "🚀 Attempting Terraform Apply..."
      if ! terraform apply -auto-approve; then
        echo "⚠️ Apply failed due to 'Already Exists' error. Executing Nuclear Wipe..."
        wipe_multipass
        echo "♻️ Retrying apply on a clean slate..."
        terraform apply -auto-approve
      fi
      # Final safety check: If it still fails here, we need to see why
      if [ $? -ne 0 ]; then
        echo "❌ Critical Failure: Terraform cannot recover even after a wipe."
        exit 1
      fi
      terraform output -raw master_name > ../.master_name
  - name: "wait_for_master"
    mode: "foreground"
    script: |
      if [ ! -f .master_name ]; then echo "❌ .master_name not found"; exit 1; fi
      MASTER=$(cat .master_name)
      echo "🎻 Waiting for Master API ($MASTER)..."
      MAX_RETRIES=20
      RETRY_COUNT=0
      until multipass exec $MASTER -- bash -c "sudo KUBECONFIG=/etc/kubernetes/admin.conf kubectl get nodes --request-timeout=5s" ; do
        echo "🎻 checking for Master API ($MASTER)..."
        RETRY_COUNT=$((RETRY_COUNT + 1))
        if [ $RETRY_COUNT -gt $MAX_RETRIES ]; then
          echo "❌ ERROR: Master failed to respond after $MAX_RETRIES retries: ... "
          set -x
          multipass exec $MASTER -- systemctl status kubelet | head -n 10
          set +x
        fi
        echo "⏳ Attempt $RETRY_COUNT/$MAX_RETRIES..."
        sleep 5
      done
      echo "🚀 Master is online."
  - name: "check_workers"
    script: |
      MASTER=$(cat .master_name)
      echo "🔍 Scanning workers..."
      > .nodes_to_join
      # DEFENSE: List all workers regardless of status (Deleted "Running" filter)
      WORKERS=$(multipass list --format csv | awk -F, 'NR>1 && $1 ~ /worker/ {print $1}' | xargs)
      echo "WORKERS=_${WORKERS}_"
      for NODE in $WORKERS; do
        echo "🧐 Checking $NODE..."
        # Ensure the node is actually powered on
        VM_STATUS=$(multipass info $NODE --format csv | awk -F, 'NR>1 {print $2}')
        echo "VM_STATUS=_${VM_STATUS}_"
        if [ "$VM_STATUS" != "Running" ]; then
          echo "⁉️ $NODE is $VM_STATUS. Attempting to start..."
          multipass start $NODE || true
        fi
        echo "🧐 Checking if $NODE is registered in K8s..."
        NODE_CHECK=$(multipass exec k8s-master -- kubectl get node "$NODE" 2>&1)
        echo "NODE_CHECK=_${NODE_CHECK}_"
        if [[ $NODE_CHECK == *"not found"* ]]; then
            echo "❌ $NODE is not yet registered in the cluster. (VM is up, but K8s isn't ready)."
            echo "$NODE" >> .nodes_to_join
        elif [[ $NODE_CHECK == *"$NODE"* ]]; then
            READY_STATUS=$(multipass exec k8s-master -- kubectl get node "$NODE" -o "jsonpath={.status.conditions[?(@.type=='Ready')].status}")
            echo "READY_STATUS=_${READY_STATUS}_"
            if [ "$READY_STATUS" == "True" ]; then
                echo "✅ $NODE is Ready!"
            else
                echo "⏳ $NODE exists but status is: $READY_STATUS"
                echo "$NODE" >> .nodes_to_join
            fi
        else
            echo "❓ Unknown state for $NODE: $NODE_CHECK"
            echo "$NODE" >> .nodes_to_join
        fi
      done
  - name: "join_workers"
    script: |
      MASTER=$(cat .master_name)
      if [ ! -s .nodes_to_join ]; then
        echo "🎉 All workers are healthy. No action needed."
        exit 0
      fi
      # Loop through the list we just made and call the script
      for NODE in $(cat .nodes_to_join); do
        echo "🛠️  Starting repair script for $NODE..."
        bash ./scripts/join-worker.sh "$NODE" "$MASTER"
      done
      echo "✅ All worker repairs finished."
      rm .nodes_to_join
      echo "✅ Worker processing complete."
  - name: "wait_for_cluster"
    script: "bash ./scripts/wait-for-cluster.sh"
    mode: "foreground"
  - name: "apps"
    mode: "foreground"
    script: "helmfile apply -f ./yaml/helmfile.yaml"
  - name: "demo_source"
    script: "./demo"
    mode: "source"
  - name: "cleanup"
    script: "cleanup"
    mode: "foreground"
